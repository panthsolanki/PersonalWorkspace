import styled from "styled-components";
import { ColumnDiv } from "baseStyled";
export const ContainerWrapper = styled(ColumnDiv) `
    width : 30%;
    margin : 5% auto;
`;

export const LoginWrapper = styled(ColumnDiv) `
    // align-items : center;
`;